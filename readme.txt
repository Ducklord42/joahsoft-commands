run the download.sh once with:
/bin/bash download.sh

Then it will check for updates every time you open the terminal

THIS WILL CHANGE YOUR ~/.bashrc or ~/.bash_profile IT WILL ONLY APPEND TO THAT FILE THOUGH

REQUIREMENTS:
 - bash config file as ~/.bashrc
 - git
 - linux file system
